using System;
using System.Collections.Generic;
using System.Linq;
using System.Web;

namespace PersoLib_DAL
{
    internal static partial class DAL
    {
        internal class Livros
        {
            internal int InserirNovoLivro(Entity.Livros aoLivro)
            {
                int liResult = -1;

                // Chamada criada para auxílio na camada superior (Business)

                return liResult;
            }

            internal int AtualizarDadosLivro(Entity.Livros aoLivro)
            {
                int liResult = -1;

                // Chamada criada para auxílio na camada superior (Business)

                return liResult;
            }

            internal int RemoverLivro(Entity.Livros aoLivro)
            {
                int liResult = -1;

                // Chamada criada para auxílio na camada superior (Business)

                return liResult;
            }
        }
    }
}